# 基础依赖
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6
pandas>=2.0.0
numpy>=1.26.0
matplotlib>=3.7.1
scipy>=1.10.0

# 指定正确版本的crewai（必须包含flow模块）
# crewai==0.28.0 这个版本没有flow模块
crewai>=0.100.0  # 使用新版本，包含flow模块

# 其他AI依赖
langchain>=0.1.0
langchain-core>=0.1.0
langsmith>=0.0.86

# 其他必要依赖
pyyaml>=6.0.0
python-dotenv>=1.0.0

# 添加到requirements.txt
setuptools>=65.0.0

# 添加crewai-tools
crewai-tools

firecrawl-py
